const express = require('express');
const router = express.Router();

// This file can be used for other payment gateway integrations in the future (e.g., Stripe, PayPal).
// For now, it exports an empty router as M-Pesa routes are handled in mpesaRoutes.js.

module.exports = router;
